require("telescope").setup {
    defaults = {
        layaout_strategy = "horizontal",
        layaout_config = {
            horizontal = {
                preview_cutoff = 0,
            },
        },
    },
}
